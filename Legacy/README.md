# Legacy

Earlier working material: per-week scripts and results, input data, the original
docs, and the flyer. Kept for reference only. Current work is in
`carbon_intensity_toolkit/` (workflow) and `dashboard/` (app).
